# GitHub学習帳

co-dev-1st プロジェクトを作る中で実際に使った操作をもとに、よく出てくる用語を作業の流れ順にまとめたものです。わからなくなったらここに戻ってきてください。新しい用語が出てきたら、この形式で追記していきましょう。

## 土台になる言葉(最初に1回だけ関わる)

### リポジトリ (repository)
プロジェクトのファイル一式と、その変更履歴をまるごと保存しておく「箱」。このプロジェクトでは `HTZY87/co-dev-1st` がそのリポジトリにあたります。

### クローン (clone)
GitHub上にあるリポジトリを、自分の作業環境にまるごとコピーしてくること。

```
git clone https://github.com/HTZY87/co-dev-1st.git
```

### リモート / origin (remote / origin)
手元の作業コピーと、GitHub上の本体をつなぐ「連絡先」。`origin` は、そのGitHub上の場所につけられた標準の呼び名です。

例: origin → `https://github.com/HTZY87/co-dev-1st`

### README (README.md)
リポジトリを開いたときに最初に表示される案内ファイル。目的・ルール・使い方などを書いておく、いわば「表紙」です。

## 変更を記録する言葉(ファイルをいじるたびに関わる)

### コミット (commit)
「ここまでの変更を1つの区切りとして記録する」操作。あとから履歴を見返せるように、何を変えたか一言メッセージを添えます。

例: `初回コミット: READMEを追加`

### .gitignore (ignore)
「このファイル・フォルダは記録(コミット)しなくていい」とGitに伝えるための除外リスト。自動生成されるファイルや、パスワードなど秘密情報の入ったファイルを誤って記録しないための保険です。

例: `node_modules/`、`.env` を除外対象に指定

## 分けて作業する言葉(2人で同時に作業するための仕組み)

### ブランチ (branch)
「本体はそのままにして、自分だけの作業スペースで試す」ための分岐。1本の木から枝が分かれるイメージで、枝の上でどれだけ変更しても本体には影響しません。

例: `feature/hello-world` というブランチでサンプルを作成

### main ブランチ (main)
「完成した状態」を保つ、リポジトリの本体となるブランチ。このプロジェクトのルールでは、ここには直接書き込まず、必ずPRを通して変更します。

## 共有する言葉(手元の変更をGitHubに反映する)

### プッシュ (push)
記録した変更(コミット)を、GitHub上のリポジトリに送り届けること。プッシュするまでは、変更は自分の手元にあるだけです。

例: `README.md` と `.gitignore` を `main` にプッシュ

### プル / フェッチ (pull / fetch)
プッシュの逆で、GitHub上にある最新の変更を自分の手元に取り込むこと。相手が先にプッシュしていたら、作業前にプルして最新の状態にしておきます。

## まとめる言葉(2人の作業を1つにする)

### プルリクエスト・PR (pull request)
「このブランチの変更を、mainに取り込んでいいですか?」という提案書のようなもの。変更内容を相手が見てから取り込めるように、PRを通してレビューします。

例: `feature/hello-world` → `main` へのPR

### レビュー (review)
PRの内容を相手に確認してもらうこと。コメントを付けたり、「問題なし(Approve)」を出したりして、取り込んでよいか判断します。

### マージ (merge)
レビューが済んだPRの変更を、実際にmainブランチへ取り込む操作。これで枝分かれしていた作業が本体に合流します。

### コンフリクト (conflict)
2人が同じ場所を別々に変更していて、Gitがどちらを採用すればいいか自動で判断できない状態。エラーではなく「人に確認したい」というGitからのサインなので、該当箇所を見てどちらを残すか決めれば解決します。

### コラボレーター (collaborator)
そのリポジトリに書き込み権限を持つ、一緒に作業する仲間。Settings → Collaborators から招待できます。
