# 立石のアプリ案(作るものの候補)

このリポジトリで実際に作るアプリの候補を、立石がまとめたものです。

選定の基準は **「勉強になって、かつ取り組みやすい」** の両立です。具体的には次の3点を満たすものだけを選びました。

- **半日〜2週間で動くものができる**: 途中で力尽きない
- **新しい概念が1つずつ学べる**: 通信、データ保存、自動化など、実際のアプリ開発で必ず出てくる部分
- **2人で分担できる**: 担当を分ければ同時に作業でき、それがそのままブランチとPRの単位になる

大事なのは **車輪の再発明をしないこと**です。「Gitやデータベースを一から自作する」ような題材は勉強にはなりますが数ヶ月かかります。ここでは既存のライブラリやAPIを遠慮なく使い、そのぶん「外の世界とつなぐ」「データを保存する」といった実用的な部分に集中します。

---

## 案1: 天気予報アプリ(外部APIに触れる)

地名を入れると天気を表示するアプリ。Open-Meteoなど、登録不要で使える無料APIから始められます。

**学べること**

- **非同期通信**: `fetch` と `async/await`。「結果が返ってくるのを待つ」処理の書き方は、ここから先ずっと使います
- **JSONの読み解き**: 他人が設計したデータ構造から、必要な値を取り出す。実務でよくある作業そのもの
- **エラー処理**: 通信が失敗した、地名が見つからない、APIが落ちている。「うまくいかない場合」を考える習慣がつく
- **APIキーの扱い**: キーが必要なAPIを使う場合、`.env`に書いて`.gitignore`で除外する。GLOSSARY.mdの`.gitignore`の説明が、ここで実感に変わります

**2人の分担**: 1人がAPI取得とデータ整形、1人が画面表示。「取得結果をどんな形で渡すか」だけ先に決めれば、あとは独立して進められます。

**進め方**: ① 固定の地名で今日の天気 → ② 地名を検索できる → ③ 週間予報 → ④ エラー表示 → ⑤ 見た目を整える

**最小版が動くまで**: 1日

---

## 案2: 2人で共有するTODO・メモアプリ(サーバーとデータベース)

ブラウザの中だけに保存されるアプリを、「2人で同じデータを共有できる」アプリに進化させる。Node.js(Express)+ SQLite のような構成。

**学べること**

- **クライアントとサーバーの分離**: どちらに何を書くべきかという、Web開発で一番大事な感覚
- **REST APIの設計**: GET/POST/PUT/DELETE と、追加・読み出し・更新・削除(CRUD)の対応
- **データの永続化**: データベースとSQLの基本
- **デプロイ**: 手元ではなく、インターネット上で動かす

**ここが分水嶺**: ブラウザだけの保存(localStorage)は「自分の中だけ」でした。サーバーを持った瞬間、アプリは初めて2人で使えるものになります。**作れるものの幅が一番大きく広がるのがこのステップ**です。

**2人の分担**: サーバー担当と画面担当。**APIの仕様(どのURLにアクセスすると、どんなJSONが返るか)を先にPRで合意してから**並行作業に入る形になるので、設計をレビューする練習としても理想的です。

**進め方**: ① 一覧を返すAPIだけ作る → ② 追加できる → ③ 更新・削除 → ④ DBに保存 → ⑤ デプロイ

**最小版が動くまで**: 1〜2週間

---

## 案3: 自作コマンドラインツール(このリポジトリを便利にする)

自分たちの作業を楽にする小さなCLIツール。たとえば、

- `diary new` で日付入りの日記ファイルを雛形から作る
- GLOSSARY.mdを読んで目次を自動生成する
- リポジトリ内のリンク切れを探す

**学べること**: コマンドライン引数の受け取り、ファイルの読み書き、正規表現によるテキスト処理。さらに **GitHub Actionsで自動実行**すれば、「PRを出すと目次が自動で更新される」という自動化まで到達できます。

**なぜ良いか**: 題材が自分たちのリポジトリなので、完成した瞬間から毎日使うことになります。使うから改善したくなる、という良い循環が生まれます。

**2人の分担**: サブコマンド単位で分ける(1人が`new`、1人が`toc`)。ファイルごと分かれるので、コンフリクトもほぼ起きません。

**進め方**: ① 引数なしの固定動作 → ② 引数を受け取る → ③ 複数のサブコマンド → ④ テストを書く → ⑤ Actionsで自動実行

**最小版が動くまで**: 半日

---

## 案4: 共同開発ダッシュボード(GitHub APIを使う)

自分たちのリポジトリのコミット数、PR数、レビュー回数などを取得して、グラフで見せるページ。

**学べること**: 認証つきAPI(トークンの扱い)、ページネーション(一度に全部は取得できない仕組み)、データの集計、グラフ描画ライブラリ、日付の扱い(意外と厄介です)。

**なぜ良いか**: **このリポジトリでやっている練習そのものが可視化されます**。PRを出すほどグラフが伸びるので、「共同開発に慣れる」という当初の目的と完全に噛み合います。

**2人の分担**: データ取得・集計担当と、グラフ・画面担当。

**進め方**: ① コミット数を取得して表示 → ② 週ごとに集計 → ③ グラフ化 → ④ PRやレビューも数える → ⑤ 2人を比較表示

**最小版が動くまで**: 1週間

---

## 案5: Claude APIを使ったアプリ

たとえば、日記をまとめて月次サマリを作る、GLOSSARY.mdからクイズを自動生成する、変更内容からコミットメッセージを提案する、など。

**学べること**: APIへのリクエストとレスポンスの扱い、プロンプト設計(指示の書き方で結果がどう変わるか)、待ち時間が長い処理の見せ方、失敗時のリトライ、トークンとコストの考え方。

**特に大事な学び**: APIキーをうっかりGitHubに上げてしまう事故は非常に多いです。**キーは`.gitignore`で除外し、フロントエンドに置かずサーバー経由で呼ぶ**という基本を実地で身につける、良い機会になります(案2と組み合わせると自然にその形になります)。

**2人の分担**: プロンプト設計・API呼び出し担当と、UI・結果表示担当。

**進め方**: ① 固定の文章をAPIに投げて結果表示 → ② 入力を受け取る → ③ プロンプトを改善する → ④ 待ち時間とエラーの表示 → ⑤ 結果を保存

**最小版が動くまで**: 1日

---

## 5案の比較

| 案 | 最小版が動くまで | 主に学べること |
|---|---|---|
| 1. 天気アプリ | 1日 | 非同期通信・JSON・エラー処理 |
| 2. 共有TODO | 1〜2週間 | サーバー・データベース・API設計 |
| 3. CLIツール | 半日 | ファイル操作・自動化・CI |
| 4. ダッシュボード | 1週間 | API認証・データ集計・可視化 |
| 5. Claude APIアプリ | 1日 | API連携・プロンプト・秘密情報の管理 |

## おすすめの順番

1. **案3(CLIツール)** — 半日で動くのに学べることが多く、題材がこのリポジトリ自身なので「何を作るか」で悩まずに済む。完成した瞬間から毎日使う道具になります
2. **案1か案5** — 外部と通信するアプリの作り方を覚える
3. **案2(共有TODO)** — 最大の山場。ここまで来れば、世の中のWebアプリの仕組みはだいたい説明できるようになります

## 次にやること

2人でこのファイルを読み、どれから作るかを決める。決まったら、その案の設計メモ(どんな画面にするか、どんなデータを扱うか、担当をどう分けるか)を書いたPRを出すところから始める。
