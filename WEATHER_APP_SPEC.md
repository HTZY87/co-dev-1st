# 天気予報アプリ 仕様書(たたき台)

`TATEISHI_APP_IDEAS.md` の案1を作ることに決めたので、その仕様をまとめたものです。
このファイル自体もレビュー対象です。「ここはこうしたい」があればPRのコメントで指摘してください。合意できたらマージし、以後はこの仕様書を見ながら実装します(実装しながら変わった点は、都度この仕様書を直すPRを出します)。

**方針: 学びを最優先にする。** 最短で動くものを作るだけでなく、実務のWebアプリ開発で必ず出てくる要素(非同期の競合、キャッシュ、テスト、CI)を意図的にスコープへ入れています。そのぶん完成までは長めです。

---

## 1. 目的

- 地名を入力すると、その場所の今日の天気と週間予報を表示するWebアプリを作る
- 2人で担当を分け、ブランチ → PR → レビュー → マージ の流れで開発する練習をする
- 題材を通して次を学ぶ:
  - **非同期通信の基本**: fetch / async-await / JSONの読み解き
  - **非同期の競合(レースコンディション)**: 検索を連打したとき、古い結果が後から届いて画面を上書きする問題と、その対策(AbortController)
  - **キャッシュ**: 同じ問い合わせを繰り返さない仕組みと、キャッシュの「賞味期限」の考え方
  - **テストとCI**: ロジックを純粋関数に切り出してテストを書き、GitHub ActionsでPRごとに自動実行する

## 2. 完成イメージ

```
┌──────────────────────────────────┐
│  お天気アプリ                       │
│                                  │
│  [ 東京           ] [検索]        │
│  ★お気に入り: 東京 | 大阪 | 札幌    │
│                                  │
│  東京都 東京(8/11 14:00 取得)     │
│  ☀ 晴れ  31℃   風速 3.2 m/s      │
│                                  │
│  ── 週間予報 ──                   │
│  火  ☀  33℃ / 25℃  ☔10%        │
│  水  ☁  30℃ / 24℃  ☔30%        │
│  木  🌧  27℃ / 23℃  ☔80%        │
│  ...                             │
│                                  │
│  ── 気温の推移(グラフ)──          │
│  ▁▃▅▇▅▃▁                         │
└──────────────────────────────────┘
```

## 3. スコープ

### やること(この順で作る。1ステップ = 1PR)

| # | 内容 | このステップで学ぶこと | 担当 |
|---|---|---|---|
| 0 | この仕様書のレビューと合意 | 設計を先に合意する進め方 | 2人 |
| 1 | 固定の地名(東京)で今日の天気を表示 | fetch / async-await / JSON | 2人で分担 |
| 2 | 地名検索。候補が複数あれば一覧から選ぶ | 検索→選択という2段階のUI状態管理 | 2人で分担 |
| 3 | 週間予報(7日分、降水確率つき) | 配列データの整形と表示 | 表示担当 |
| 4 | エラー表示・読み込み中表示・**検索連打対策** | エラー設計とレースコンディション | 2人で分担 |
| 5 | キャッシュ(同じ地点は10分間再取得しない) | キャッシュと有効期限 | データ担当 |
| 6 | お気に入り地点の保存・呼び出し | localStorageによる永続化 | 表示担当 |
| 7 | ロジックのテストを書き、GitHub Actionsで自動実行 | 純粋関数・テスト・CI | 2人で分担 |
| 8 | 見た目を整える + GitHub Pages公開 | デプロイ、main=本番の感覚 | 表示担当 |
| 9 | (ストレッチ)週間気温の折れ線グラフをSVGで自作 | 座標計算とSVG描画 | どちらか |

ステップ7以降は順序を入れ替えてもよいです(テストはもっと早くてもよい)。

### やらないこと(今回は範囲外)

- 現在地の自動取得(位置情報の許可まわりに時間を取られるため)
- 雨雲レーダーなどの地図表示
- グラフ描画ライブラリの使用(ステップ9は学びのためにあえて自作する)
- スマホアプリ化(ブラウザで見られれば良い)

## 4. 使う技術

- **HTML / CSS / JavaScript(ESモジュール)**。ビルドツールは使わない
- 天気データ: **Open-Meteo**(登録不要・無料・APIキー不要)
  - 地名 → 緯度経度: Geocoding API
  - 緯度経度 → 天気: Forecast API
- テスト: **Node.js標準の `node --test`**(テストライブラリの追加インストール不要)
- CI: **GitHub Actions**(PRを出すとテストが自動で走り、結果がPR画面に出る)
- 公開: GitHub Pages

**開発時の注意**: ESモジュール(`<script type="module">`)は `file://` で直接開くと動きません。開発中はリポジトリ直下で次のどちらかを実行し、ローカルサーバー経由で開きます。

```
python3 -m http.server 8000     # → http://localhost:8000/weather-app/
npx serve                        # Node.jsがある場合
```

「なぜ直接開けないのか(CORSと同一オリジン)」も、つまずいたときに調べる価値のあるテーマです。

APIキーが不要なので秘密情報の管理は今回は発生しません(キーが必要なAPIに乗り換えるときに `.env` + `.gitignore` を導入します)。

## 5. データの流れ

```
地名(文字列)
   │  Geocoding APIに問い合わせ
   ▼
候補一覧(最大5件) ──→ 1件だけならそのまま、複数なら利用者が選ぶ
   │
   ▼
緯度・経度
   │  キャッシュにあり10分以内 → APIを呼ばずキャッシュを返す
   │  なければ Forecast APIに問い合わせ
   ▼
天気データ(JSON)
   │  必要な値だけ取り出して整形(純粋関数 = テスト対象)
   ▼
画面に表示
```

### 使うAPIの例

地名検索(「東京」で検索する例。`count=5` で候補を5件まで取る):

```
https://geocoding-api.open-meteo.com/v1/search?name=東京&count=5&language=ja
```

天気取得(東京の緯度経度で、現在の天気と週間予報・降水確率を取る例):

```
https://api.open-meteo.com/v1/forecast?latitude=35.69&longitude=139.69&current=temperature_2m,weather_code,wind_speed_10m&daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max&timezone=Asia%2FTokyo
```

どちらもブラウザのアドレス欄に貼るだけで、返ってくるJSONをそのまま見られます。実装前に一度見ておくと理解が早いです。

## 6. 一番大事な取り決め: 2人の境界のデータ形式

**データ担当**が実装する関数を先に決めておきます。表示担当はこの2つを呼ぶだけ。ここさえ守れば、お互いの中身がどうなっていても独立して作業できます。

```js
// ① 地名から候補一覧を返す(最大5件)。見つからなければ空配列。
async function searchCities(query, { signal } = {}) { ... }
// 返り値の形:
// [ { label: "東京都 東京", latitude: 35.69, longitude: 139.69 }, ... ]

// ② 場所(候補の1件)から天気を返す。失敗したらエラーを投げる。
async function getWeather(location, { signal } = {}) { ... }
```

`getWeather()` が返すオブジェクトの形:

```js
{
  city: "東京都 東京",         // 表示用の地名(location.labelをそのまま)
  fetchedAt: "2026-08-11T14:00:00+09:00",  // 取得時刻(キャッシュ判定と画面表示に使う)
  current: {
    temperature: 31.2,        // 気温(℃)
    weatherCode: 0,           // 天気コード(下の表を参照)
    windSpeed: 3.2            // 風速(m/s)
  },
  daily: [                    // 今日を含む7日分
    { date: "2026-08-11", weatherCode: 0, tempMax: 33.1, tempMin: 25.0, rainChance: 10 },
    // ... 計7件(rainChanceは降水確率%)
  ]
}
```

- `{ signal }` はAbortController対応(ステップ4)のための引数です。ステップ1〜3の間は無視してかまいませんが、**引数の形だけ最初から決めておく**ことで、後から関数の呼び出し方を変えずに済みます
- この形を変えたくなったら、**先にこの仕様書を直すPRを出して合意してから**実装を変えます

### 天気コードの対応表(表示担当が使う)

Open-MeteoはWMOという国際規格のコードで天気を返します。主なもの:

| コード | 意味 | 表示 |
|---|---|---|
| 0 | 快晴 | ☀ 快晴 |
| 1〜3 | 晴れ〜曇り | 🌤 晴れ時々曇り |
| 45, 48 | 霧 | 🌫 霧 |
| 51〜55 | 霧雨 | 🌦 霧雨 |
| 61〜65 | 雨 | 🌧 雨 |
| 71〜75 | 雪 | ❄ 雪 |
| 80〜82 | にわか雨 | 🌦 にわか雨 |
| 95〜99 | 雷雨 | ⛈ 雷雨 |

上記以外のコードが来たら「─(不明)」と表示します。この変換は `utils.js` の純粋関数にして、テスト対象にします。

## 7. ファイル構成

```
weather-app/
├── index.html            … 画面の骨組み(表示担当)
├── style.css             … 見た目(表示担当)
├── api.js                … searchCities() / getWeather() とキャッシュ(データ担当)
├── utils.js              … 純粋関数置き場: 天気コード変換、日付整形、
│                            APIレスポンス→表示用データの変換(両担当・テスト対象)
├── app.js                … 検索・選択・表示の流れとお気に入り(表示担当)
└── tests/
    └── utils.test.mjs    … utils.jsのテスト(node --test で実行)

.github/
└── workflows/
    └── test.yml          … PRごとにテストを自動実行する設定
```

**utils.jsを分ける理由**: 「fetchを含む関数」はテストしにくく、「入力→出力が決まっているだけの純粋関数」はテストしやすい。テストしやすい形にロジックを寄せるのは、実務でも通用する設計の基本です。

## 8. エラー処理と非同期の方針

エラーはすべて画面上の1か所(検索欄の下)に日本語で表示します。`alert()` は使いません。

| 状況 | 挙動 |
|---|---|
| 地名が見つからない | 「該当する地名が見つかりませんでした」 |
| 通信に失敗した | 「通信に失敗しました。時間をおいて試してください」 |
| 入力が空のまま検索 | 「地名を入力してください」 |
| 読み込み中 | 「読み込み中…」を表示し、検索ボタンを無効化 |

**検索連打への対策(ステップ4の山場)**: 「大阪」で検索した直後に「札幌」で検索すると、通信の速さ次第では大阪の結果が後から届いて札幌の画面を上書きすることがあります。対策として、新しい検索を始めるときに `AbortController` で前の通信を中断します。**まず対策なしで作って、開発者ツールの通信速度制限(Slow 3G)でこのバグを実際に再現してから**直すことを推奨します。壊れ方を見てから直すと理解が段違いです。

**キャッシュの方針(ステップ5)**: 同じ緯度経度への問い合わせは、前回取得から10分以内なら通信せずキャッシュを返します。キャッシュは `api.js` の内部に持ち、表示担当からは見えない(=`getWeather()` の使い方が変わらない)ようにします。判定には `fetchedAt` を使います。

## 9. テストとCI(ステップ7)

- `utils.js` の関数それぞれについて「この入力ならこの出力」を `tests/utils.test.mjs` に書く
- 手元では `node --test weather-app/tests/` で実行
- `.github/workflows/test.yml` を追加し、PRを出すたびにGitHubが自動でテストを実行。結果はPR画面に緑のチェック/赤の✗で表示される
- **赤のままマージしない**をルールにする(ブランチ保護を設定すれば強制もできる)

テスト例(イメージ):

```js
import { test } from 'node:test';
import assert from 'node:assert';
import { weatherCodeToLabel } from '../utils.js';

test('コード0は快晴', () => {
  assert.strictEqual(weatherCodeToLabel(0), '☀ 快晴');
});

test('未知のコードは不明扱い', () => {
  assert.strictEqual(weatherCodeToLabel(999), '─(不明)');
});
```

## 10. 担当分け

| 担当 | 役割 | 主に触るファイル |
|---|---|---|
| データ担当 | searchCities / getWeather の実装、エラー送出、キャッシュ | `api.js`、`utils.js` の一部 |
| 表示担当 | HTML/CSS、検索・候補選択・結果表示、お気に入り | `index.html` `style.css` `app.js` |
| 2人 | utils.jsの純粋関数とそのテスト、CI設定 | `utils.js` `tests/` `.github/` |

どちらが何を担当するかは、このPRのコメントで決めて、決まったらこの表に名前を入れます。途中のステップで交代するのもありです(両方経験できるので、学び優先ならむしろ推奨)。

ステップ1では、表示担当は `getWeather()` がまだ無くても進められるように、**仕様書どおりの形のダミーデータを自分で書いて**画面を作ります。データ担当の実装がマージされたら、ダミーを本物に差し替えます。

## 11. 未決事項(レビューで決めたいこと)

- [ ] どちらがデータ担当・表示担当で始めるか(途中交代するか)
- [ ] デザインの方向性(シンプル寄りか、天気に合わせて背景が変わる遊び寄りか)
- [ ] ステップ9(SVGグラフ自作)をやるか、やるならステップ7の後でよいか
- [ ] キャッシュの有効期限は10分でよいか
