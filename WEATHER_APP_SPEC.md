# 天気予報アプリ 仕様書(たたき台)

`TATEISHI_APP_IDEAS.md` の案1を作ることに決めたので、その仕様をまとめたものです。
このファイル自体もレビュー対象です。「ここはこうしたい」があればPRのコメントで指摘してください。合意できたらマージし、以後はこの仕様書を見ながら実装します(実装しながら変わった点は、都度この仕様書を直すPRを出します)。

---

## 1. 目的

- 地名を入力すると、その場所の今日の天気と週間予報を表示するWebアプリを作る
- 題材を通して **非同期通信(fetch)・JSONの読み解き・エラー処理** を学ぶ
- 2人で担当を分け、ブランチ → PR → レビュー → マージ の流れで開発する練習をする

## 2. 完成イメージ

```
┌─────────────────────────────┐
│  お天気アプリ                  │
│                             │
│  [ 東京           ] [検索]   │
│                             │
│  東京都 東京                  │
│  ☀ 晴れ  31℃                │
│  風速 3.2 m/s               │
│                             │
│  ── 週間予報 ──              │
│  火  ☀  33℃ / 25℃          │
│  水  ☁  30℃ / 24℃          │
│  木  🌧  27℃ / 23℃          │
│  ...                        │
└─────────────────────────────┘
```

## 3. スコープ

### やること(この順で作る。1ステップ = 1PR)

| ステップ | 内容 | 担当 |
|---|---|---|
| 0 | この仕様書のレビューと合意 | 2人 |
| 1 | 固定の地名(東京)で今日の天気を表示 | 2人で分担(下記) |
| 2 | 地名を入力して検索できるようにする | データ担当 |
| 3 | 週間予報(7日分)を表示 | 表示担当 |
| 4 | エラー表示(地名が見つからない・通信失敗) | 2人で分担 |
| 5 | 見た目を整える(天気に合わせた背景色など) | 表示担当 |

### やらないこと(今回は範囲外)

- 現在地の自動取得(位置情報の許可まわりが面倒なので、まずは手入力)
- お気に入り地点の保存(やるとしてもステップ5より後)
- 雨雲レーダーなどの地図表示
- スマホアプリ化(ブラウザで見られれば良い)

## 4. 使う技術

- **HTML / CSS / JavaScript のみ**(ビルドツールなし。`index.html` を開けば動く)
- 天気データ: **Open-Meteo**(登録不要・無料・APIキー不要)
  - 地名 → 緯度経度: Geocoding API
  - 緯度経度 → 天気: Forecast API
- 公開: GitHub Pages(mainにマージすると本番に反映)

APIキーが不要なので、秘密情報の管理は今回は発生しません(キーが必要なAPIに乗り換えるときに `.env` + `.gitignore` を導入します)。

## 5. データの流れ

```
地名(文字列)
   │  Geocoding APIに問い合わせ
   ▼
緯度・経度(数値2つ)
   │  Forecast APIに問い合わせ
   ▼
天気データ(JSON)
   │  必要な値だけ取り出して整形
   ▼
画面に表示
```

### 使うAPIの例

地名検索(「東京」で検索する例):

```
https://geocoding-api.open-meteo.com/v1/search?name=東京&count=1&language=ja
```

天気取得(東京の緯度経度で、現在の天気と週間予報を取る例):

```
https://api.open-meteo.com/v1/forecast?latitude=35.69&longitude=139.69&current=temperature_2m,weather_code,wind_speed_10m&daily=weather_code,temperature_2m_max,temperature_2m_min&timezone=Asia%2FTokyo
```

どちらもブラウザのアドレス欄に貼るだけで、返ってくるJSONをそのまま見られます。実装前に一度見ておくと理解が早いです。

## 6. 一番大事な取り決め: 2人の境界のデータ形式

**データ担当**が作る関数と、**表示担当**が使う関数の境目を、先に1つの関数として決めておきます。ここさえ守れば、お互いの中身がどうなっていても独立して作業できます。

```js
// データ担当が実装する関数。表示担当はこれを呼ぶだけ。
// 成功したら下の形のオブジェクトを返す。失敗したらエラーを投げる。
async function getWeather(cityName) { ... }
```

`getWeather("東京")` が返すオブジェクトの形:

```js
{
  city: "東京都 東京",        // 表示用の地名
  current: {
    temperature: 31.2,       // 気温(℃)
    weatherCode: 0,          // 天気コード(下の表を参照)
    windSpeed: 3.2           // 風速(m/s)
  },
  daily: [                   // 今日を含む7日分
    { date: "2026-08-11", weatherCode: 0, tempMax: 33.1, tempMin: 25.0 },
    // ... 計7件
  ]
}
```

この形を変えたくなったら、**先にこの仕様書を直すPRを出して合意してから**実装を変えます。

### 天気コードの対応表(表示担当が使う)

Open-MeteoはWMOという国際規格のコードで天気を返します。主なもの:

| コード | 意味 | 表示 |
|---|---|---|
| 0 | 快晴 | ☀ 快晴 |
| 1〜3 | 晴れ〜曇り | 🌤 晴れ時々曇り |
| 45, 48 | 霧 | 🌫 霧 |
| 51〜55 | 霧雨 | 🌦 霧雨 |
| 61〜65 | 雨 | 🌧 雨 |
| 71〜75 | 雪 | ❄ 雪 |
| 80〜82 | にわか雨 | 🌦 にわか雨 |
| 95〜99 | 雷雨 | ⛈ 雷雨 |

上記以外のコードが来たら「─(不明)」と表示します。

## 7. ファイル構成

リポジトリ直下が散らからないよう、アプリはフォルダにまとめます。

```
weather-app/
├── index.html   … 画面の骨組み(表示担当)
├── style.css    … 見た目(表示担当)
├── api.js       … getWeather() の実装(データ担当)
└── app.js       … 検索ボタンと表示の処理(表示担当)
```

ファイル単位で担当が分かれているので、コンフリクトはほぼ起きない想定です。

## 8. エラー処理の方針

エラーはすべて画面上の1か所(検索欄の下)に日本語で表示します。`alert()` は使いません。

| 状況 | 表示するメッセージ |
|---|---|
| 地名が見つからない | 「該当する地名が見つかりませんでした」 |
| 通信に失敗した | 「通信に失敗しました。時間をおいて試してください」 |
| 入力が空のまま検索 | 「地名を入力してください」 |
| 読み込み中 | 「読み込み中…」(エラーではないが同じ場所に出す) |

## 9. 担当分け

| 担当 | 役割 | 主に触るファイル |
|---|---|---|
| データ担当 | API呼び出し、JSONの整形、`getWeather()` の実装とエラー送出 | `api.js` |
| 表示担当 | HTML/CSS、検索フォーム、結果とエラーの表示 | `index.html` `style.css` `app.js` |

どちらが何を担当するかは、このPRのコメントで決めて、決まったらこの表に名前を入れます。

ステップ1では、表示担当は `getWeather()` がまだ無くても進められるように、**仕様書どおりの形のダミーデータを自分で書いて**画面を作ります。データ担当の実装がマージされたら、ダミーを本物に差し替えます。

## 10. 未決事項(レビューで決めたいこと)

- [ ] どちらがデータ担当・表示担当をやるか(途中のステップで交代するのもあり)
- [ ] 週間予報に降水確率も出すか(Open-Meteoで取得可能。出すなら `getWeather()` の形に追加が必要)
- [ ] デザインの方向性(シンプル寄りか、天気に合わせて背景が変わる遊び寄りか)
- [ ] GitHub Pages公開はステップ1の直後にやるか、ステップ5まで待つか
